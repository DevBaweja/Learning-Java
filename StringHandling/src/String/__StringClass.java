package String;

public class __StringClass {
	
	// The java.lang.String class
	
	// public final class String extends Object	implements Serializable, Comparable<String>, CharSequence

	// 	String Class is immutable
	// By string literal
			// String objects are stored in a special memory area known as the "string constant pool"
			// no new objects are created if it exists already in the string constant pool
			String s1 = "Hello";
			String s2 = "Hello";

			/*
			*  Why are String Object are immutable ?
			* How to create immutable class ?
			* What is String constant pool ?
			* What code is written by the compiler if you concatenate any string by + (string concatenation operator)?
			* Diff btw StringBuffer and StringBuilder Class ? */
			
			
}
