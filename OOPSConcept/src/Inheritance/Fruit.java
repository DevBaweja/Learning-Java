package Inheritance;

interface Fruit {

	// interface has all the method default as abstract
	//  interface has all the datatypes default is public static final 
	// public so that it can be accessed anywhere
	// final as to fixed value (read only variable)
	// static so that we dont need to create it object
	
	// it is also called as pure abstract class
	
	void taste();
	void season();
	void color();
	
}
